package com.jpillion.dailyreadingplanner.ui.theme

import androidx.compose.material3.Typography

// Material 3 defaults are already a comfortable scale (bodyLarge = 16sp) and respect
// system font scaling via sp (PRD a11y baseline). Customize per-style only when needed.
val AppTypography = Typography()
