# Android / Gradle
.gradle/
build/
local.properties
*.apk
*.aab
*.keystore
*.jks

# IDE
.idea/
*.iml
.DS_Store

# Captures / logs
captures/
*.log

# Release signing (S9-T5): local key material, never committed
keystore.properties

# Local, per-machine Claude config: settings.local.json and the agents symlink
# (points to an external shared library; dangles for anyone cloning). Not app code.
.claude/
